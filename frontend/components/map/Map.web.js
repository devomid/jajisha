import { View, Text } from "react-native";

export default function Map() {
    return (
        <View style={{ flex: 1 }}>
            <Text>Map is not available on web</Text>
        </View>
    );
}